# KKG Display Check

## Repository status

- `main` is the stable v1 branch; v1 remains reference/history.
- `develop-v2` is the active v2 development branch.
- The v2 Streamlit UI is a temporary prototype/reference UI.
- The portable Python v2 backend is the foundation for the future API:
  `ProjectContext` → `ResolvedDisplay` → `DisplayKnowledgeProfile` → `EstimateBasis`.
